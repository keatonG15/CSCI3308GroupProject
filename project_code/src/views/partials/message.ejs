<% if (locals.message) { %>
    <div
      style="text-align: center;" class="alert alert-<%- locals.error ? 'danger' : 'primary' %>"
      role="alert alert-primary">
      <%= message %>
    </div>
    <% } %>
    